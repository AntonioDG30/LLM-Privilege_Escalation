# === Ambiente Python (rigenerabile da setup/requirements.txt) ===
.venv/
venv/
__pycache__/
*.pyc

# === Segreti — MAI versionare ===
.env
*.env
**/secrets.local
*.key
*.pem

# === VM: dischi e file macchina (restano in OneDrive, fuori dal versioning) ===
*.vdi
*.vmdk
*.vbox
*.vbox-prev
*.ova
*.iso
*.nvram
*.sav

# === Dati grezzi voluminosi (il DB ~51M sta su OneDrive + nella consegna, non in git) ===
*.sqlite3
*.sqlite3-wal
*.sqlite3-journal
# risultati/raw/

# === Output rigenerabile con path assoluti locali (contiene lo username della macchina) ===
risultati/analisi.txt

# === File di lavoro interni (NON parte della consegna al docente) ===
# gestione/ = log/stato di progetto (scaffolding nostro); memory/ = preferenze di Claude.
# Restano su disco/OneDrive ma fuori dal versionamento e dalla consegna.
gestione/
memory/

# === File di sistema / OneDrive ===
Thumbs.db
desktop.ini
.DS_Store
*.tmp
